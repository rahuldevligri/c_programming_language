/* Write a program that accepts two integers from the user 
and calculate the sum of two integers.? */

#include<stdio.h>

int main()
{
    int a,b;
        printf("Input The First Integer ");
        scanf("%d", &a);
        printf("Input The Second Integer ");
        scanf("%d", &b);

        printf("Sum of the two integers = %d ",a + b);
 return 0;
}