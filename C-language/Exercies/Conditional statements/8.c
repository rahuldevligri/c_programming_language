#include <stdio.h>

int main(){
int x=5;
    if(x=4)
      printf("Hello %d",x);
    else
       printf("Hi %d",x);  
    return 0;
}
//Every Non Zero Value is TURE
//Zero is FALSE
// = <-- Assignment operator --> Value to be stored in a variable
// == <-- Equalto operator --> for comparising